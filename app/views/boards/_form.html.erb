<%= form_with(model: board, local: true) do |form| %>
  <% if board.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(board.errors.count, "error") %> prohibited this board from being saved:</h2>

      <ul>
        <% board.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

<div class="container">
<div class="row">

  <div class="field col">
    <%= form.label :accomplishment %>
    <%= form.text_area :accomplishment %>
  </div>

  <div class="field col">
    <%= form.label :challenges %>
    <%= form.text_area :challenges %>
  </div>

  <div class="w-100"></div>


  <div class="field col">
    <%= form.label :task %>
    <%= form.text_area :task %>
  </div>

  <div class="field col">
    <%= form.label :resources %>
    <%= form.text_area :resources %>
  </div>

</div>
</div>

  <div class="actions-boards">
    <%= form.submit %>
  </div>
<% end %>